function(element, content, append) {
	if(content === undefined)
	{
		// Return content
	}
	else
	{
		if(append === undefined || append === false)
		{
			// Replace content
		}
		else if(append === true)
		{
			// Append content
		}
	}
};
